import 'package:flutter/material.dart';

class MainInvestments extends StatelessWidget {
  const MainInvestments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Gestión de Inversiones');
  }
}
